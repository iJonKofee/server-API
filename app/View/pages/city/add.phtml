<?= Phalcon\Tag::javascriptInclude('pages/js/pages.min.js');?>
<form role="form" id="addFormCity">
  <div class="row">
    <div class="col-sm-12">
      <div class="form-group form-group-default required">
        <label>Name</label>
        <input name="name" type="text" class="form-control" required>
      </div>
    </div>
  </div>
  <button class="hide" type="submit"></button>
</form>

<script>
    $('#submitAddCity').unbind('click');
    $('#submitAddCity').click(function(){
    	$('#addFormCity button[type="submit"]').trigger('click');
    });
    
    $('#addFormCity').unbind('submit');
    $('#addFormCity').submit(function(e)
    {
		e.preventDefault();
		url = '/city/add';
		$.post(
			url,
			$(this).serialize(),
			function(data)
			{
				var data = JSON.parse(data)
				
				table.addRow(data.id, data.name);
				$('#addModal').modal('hide');
			}
		);
	});
</script>